[package]
name = "infr-prof-rt"
description = "Runtime for INFR_PROFILE build-time instrumentation (thread-local span accounting + exit report)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
